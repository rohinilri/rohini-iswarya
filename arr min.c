#include<stdio.h> 
int main() 
{
   int a[30],i,num,min; 
   printf("Enter no of elements");
   scanf("%d", &num);  
   printf("Enter the elements")
   scanf("%d", &a[i]);
   }
   min=a[i]; 
   for(i=0;i<n;i++) 
   {
      if(a[i]>min)
      {
         min= a[i];
      }
   } 
   printf("min Element",max); 
   return 0;
}
