#include<stdio.h>
#include<string.h>
int main()
{
char str[50],s1[50];
printf("enter string:");
scanf("%s",&str);
if(s1==strrev(str))
printf("yes it is palindrome");
else
printf("not a palindrome");
}
